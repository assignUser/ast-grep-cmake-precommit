id: avoid-add-definitions
message: Avoid using add_definitions(). Use target_compile_definitions() instead for target-specific definitions.
severity: warning
language: cmake
rule:
  pattern: add_definitions($ARGS)
